require(['main'], function() {

    require.config({
        paths: {
            'jquery': '../lib/jquery/jquery',
            'lodash': '../lib/lodash/lodash',
            'backbone': '../lib/backbone/backbone'
        }
    });

});
